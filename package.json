{
  "name": "node-mongodb-restapi",
  "version": "1.0.0",
  "description": "",
  "main": "./src/server/server.ts",
  "scripts": {
    "start": "npm run build:live",
    "build": "tsc -p .",
    "build:live": "nodemon --watch src --exec ts-node src/server/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.6.0",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "@types/express": "^4.17.7",
    "express": "^4.17.1",
    "nodemon": "^2.0.4",
    "ts-node": "^9.0.0",
    "tslint": "^6.1.3"
  }
}
